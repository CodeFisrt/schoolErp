import { HighlightTrDirective } from './highlight-tr.directive';

describe('HighlightTrDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTrDirective();
    expect(directive).toBeTruthy();
  });
});
